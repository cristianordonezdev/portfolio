import { projects } from '../libs/utils';
import { describe, it, expect } from 'vitest';

describe('utils.js', () => {
  it('functionName should behave correctly', () => {
    // const result = projects;
    // expect(result.is).toBe(isArr);
  });
});
